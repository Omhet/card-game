import React, { FunctionComponent } from 'react';
import { Arena } from '../components/Arena';

export const ArenaContainer: FunctionComponent = () => {
  return <Arena playerCards={[]} centerCards={[]} opponentCards={[]} />
};
